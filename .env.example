# =============================================================================
# SitePod Environment Variables
# =============================================================================
# Copy this file to .env and modify as needed
# =============================================================================

# -----------------------------------------------------------------------------
# Required Configuration
# -----------------------------------------------------------------------------

# Your base domain (required)
# Subdomains will be: {project}.{domain}, {project}.beta.{domain}
SITEPOD_DOMAIN=sitepod.example.com

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------

# Storage type: local, s3, oss, r2
SITEPOD_STORAGE_TYPE=local

# S3-compatible storage (required if SITEPOD_STORAGE_TYPE is not "local")
# Works with AWS S3, Cloudflare R2, Aliyun OSS, MinIO, etc.
# SITEPOD_S3_BUCKET=your-bucket-name
# SITEPOD_S3_REGION=us-east-1
# SITEPOD_S3_ENDPOINT=https://s3.amazonaws.com

# AWS credentials (or compatible)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# -----------------------------------------------------------------------------
# Admin Configuration (Optional)
# -----------------------------------------------------------------------------

# Admin account credentials (auto-generated if not set)
# SITEPOD_ADMIN_EMAIL=admin@example.com
# SITEPOD_ADMIN_PASSWORD=your-secure-password

# System email for internal operations
# SITEPOD_SYSTEM_EMAIL=system@sitepod.local

# -----------------------------------------------------------------------------
# Quota Limits (Optional)
# -----------------------------------------------------------------------------

# Maximum files per deployment (default: 10000)
# SITEPOD_MAX_FILES_PER_DEPLOY=10000

# Maximum single file size in bytes (default: 100MB)
# SITEPOD_MAX_FILE_SIZE=104857600

# Maximum total deployment size in bytes (default: 500MB)
# SITEPOD_MAX_DEPLOY_SIZE=524288000

# Maximum projects per user (default: 100)
# SITEPOD_MAX_PROJECTS_PER_USER=100

# Anonymous user limits (default: 5 projects, 50MB per deploy)
# SITEPOD_ANON_MAX_PROJECTS=5
# SITEPOD_ANON_MAX_DEPLOY_SIZE=52428800

# -----------------------------------------------------------------------------
# Cloudflare DNS Challenge (for wildcard certificates)
# -----------------------------------------------------------------------------

# Cloudflare API token with Zone:DNS:Edit permission
# Required only when using Caddyfile.wildcard
# CF_API_TOKEN=your-cloudflare-api-token
